# Alembic: Database migrations (version control for SQLAlchemy models)
alembic==1.17.2

# Astroid: Static code analysis dependency for Pylint
astroid==4.0.2

# Colorama: Adds colored output to terminal (used by Pylint)
colorama==0.4.6

# Dill: Serialization library, can be used for advanced object persistence (optional)
dill==0.4.0

# Greenlet: Used by SQLAlchemy for async support
greenlet==3.3.0

# isort: Automatically sorts imports in Python files (optional, dev tool)
isort==7.0.0

# Mako: Template engine used by Alembic for migration scripts
Mako==1.3.10

# MarkupSafe: Dependency of Mako (for safe string handling in templates)
MarkupSafe==3.0.3

# mccabe: Complexity checker used by Pylint
mccabe==0.7.0

# MySQL connector: Connects Python to MySQL databases
mysql-connector-python==9.5.0

# platformdirs: Helps tools locate config/data directories (used by some packages)
platformdirs==4.5.0

#pyinstaller: Package Python applications into standalone executables (optional, dev tool)
pyinstaller==5.13.1

# Pylint: Linter for Python code
pylint==4.0.4

# PyMySQL: Alternative MySQL driver (can choose either PyMySQL or mysql-connector-python)
PyMySQL==1.1.2

# python-dotenv: Loads environment variables from .env files
python-dotenv==1.2.1

# SQLAlchemy: ORM and database toolkit
SQLAlchemy==2.0.44

# tomlkit: Manipulate TOML files (used by some dev tools)
tomlkit==0.13.3

# typing_extensions: Backport of new type hints for older Python versions
typing_extensions==4.15.0

# uv: Unknown / possibly unused (could be a dev tool or CLI helper)
uv==0.9.15
